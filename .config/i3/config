#     _ _____
#    (_)__  /
#   / / /_ < 
#  / /___/ / 
# /_//____/  

# I3 CONFIG (~/.config/i3/config)
# -------------------------------

# #---Basic definitions---# #

set $mod Mod4
set $borders 2
set $files thunar
set $email thunderbird
set $terminal alacritty
set $browser google-chrome
set $inner_gaps 14
set $outer_gaps -2

# #---Autostart applications---# #

# exec --no-startup-id [...]
# exec_always --no-startup-id [...]
exec --no-startup-id /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1
exec --no-startup-id blueman-applet
exec --no-startup-id copyq
exec --no-startup-id ibus-daemon -drx
exec --no-startup-id nitrogen --restore; sleep 1; picom -b
exec --no-startup-id nm-applet
exec --no-startup-id start-pulseaudio-x11
exec --no-startup-id volumeicon
exec --no-startup-id xfce4-power-manager
exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# #---The look and feel---# #

# Set default desktop layout (default is tiling)
# workspace_layout tabbed <stacking|tabbed>

# Configure border style <normal|1pixel|pixel xx|none|pixel>
default_border pixel $borders
default_floating_border pixel $borders

# Hide borders
hide_edge_borders none

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Ubuntu Regular 11

# Use Mouse+$mod to drag and resize floating windows
floating_modifier $mod

# If your mouse is usually in your way, you might want to uncomment this
# focus_follows_mouse no

# Workspace back and forth (with/without active container)
# workspace_auto_back_and_forth yes

# Switch to workspace with urgent window automatically
# focus_on_window_activation focus

# Set inner/outer gaps
gaps inner $inner_gaps
gaps outer $outer_gaps

# Smart gaps (gaps used if only more than one container on the workspace)
# smart_gaps on

# Smart borders (draw borders around container only if it is not the only container on this workspace) 
# on|no_gaps (on=always activate and no_gaps=only activate if the gap size to the edge of the screen is 0)
smart_borders on

# Theme colors
set $color00 #2E3440
set $color01 #3B4252
set $color02 #434C5E
set $color03 #4C566A
set $color04 #D8DEE9
set $color05 #E5E9F0
set $color06 #ECEFF4
set $color07 #8FBCBB
set $color08 #88C0D0
set $color09 #81A1C1
set $color10 #5E81AC
set $color11 #BF616A
set $color12 #D08770
set $color13 #EBCB8B
set $color14 #A3BE8C
set $color15 #B48EAD
set $color16 #57637A
set $color17 #8A95AB
set $color18 #A1BCD7
set $color19 #B8CCE1

# Window decorations
# class                   border   backgro. text     indicat. child_border
  client.focused          $color19 $color18 $color01 $color13
  client.focused_inactive $color00 $color01 $color18 $color13
  client.unfocused        $color00 $color01 $color18 $color13
  client.urgent           $color12 $color11 $color06 $color13

# #---Letter key bindings---# #

# bindsym $mod+q
bindsym $mod+Shift+q    kill

bindsym $mod+w          layout tabbed
bindsym $mod+Shift+w    exec --no-startup-id $browser

bindsym $mod+e          layout toggle split
bindsym $mod+Shift+e    exec --no-startup-id $email

bindsym $mod+r          mode "resize"
bindsym $mod+Shift+r    restart

bindsym $mod+t          exec --no-startup-id pkill picom
bindsym $mod+Shift+t    exec --no-startup-id picom -b

bindsym $mod+y          border pixel $borders
bindsym $mod+Shift+y    exec --no-startup-id copyq toggle

bindsym $mod+u          border none
bindsym $mod+Shift+u    [urgent=latest] focus

bindsym $mod+i          split h ;exec --no-startup-id notify-send 'Window' 'Tile horizontally'
bindsym $mod+Shift+i    --release exec --no-startup-id "pkill nm-applet; sleep 1 ; nm-applet; exec notify-send 'Network' 'Restart nm-applet'"

bindsym $mod+o          split toggle
bindsym $mod+Shift+o    exec --no-startup-id $terminal -e ranger

# bindsym $mod+p
bindsym $mod+Shift+p    exec --no-startup-id $terminal -e htop

bindsym $mod+a          focus parent
bindsym $mod+Shift+a    exec --no-startup-id pavucontrol

bindsym $mod+s          layout stacking
bindsym $mod+Shift+s    mark --add "swapee"; mode "$mode_swap"

bindsym $mod+d          exec --no-startup-id rofi -show run
# bindsym $mod+Shift+d

bindsym $mod+f          fullscreen toggle
bindsym $mod+Shift+f    exec --no-startup-id $files

bindsym $mod+g          sticky toggle
bindsym $mod+Shift+g    mode "$mode_gaps"

bindsym $mod+h          focus left
bindsym $mod+Shift+h    move left 25
bindsym $mod+Ctrl+h     workspace prev

bindsym $mod+j          focus down
bindsym $mod+Shift+j    move down 25

bindsym $mod+k          focus up
bindsym $mod+Shift+k    move up 25

bindsym $mod+l          focus right
bindsym $mod+Shift+l    move right 25
bindsym $mod+Ctrl+l     workspace next

# bindsym $mod+z
# bindsym $mod+Shift+z

# bindsym $mod+x
bindsym $mod+Shift+x    --release exec --no-startup-id xkill

bindsym $mod+c          focus child
bindsym $mod+Shift+c    reload

bindsym $mod+v          split v; exec --no-startup-id notify-send 'Window' 'Tile vertically'
# bindsym $mod+Shift+v

bindsym $mod+b          workspace back_and_forth
bindsym $mod+Shift+b    move container to workspace back_and_forth; workspace back_and_forth

bindsym $mod+n          border normal $borders
bindsym $mod+Shift+n    --release exec --no-startup-id "killall dunst; exec notify-send 'Notification' 'Restart dunst'"

bindsym $mod+m          move absolute position center
# bindsym $mod+Shift+m

# #---Workspace bindings---# #

# Workspace names
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"

bindsym $mod+1          workspace number $ws1
bindsym $mod+Shift+1    move container to workspace $ws1; workspace $ws1
bindsym $mod+Ctrl+1     move container to workspace $ws1

bindsym $mod+2          workspace number $ws2
bindsym $mod+Shift+2    move container to workspace $ws2; workspace $ws2
bindsym $mod+Ctrl+2     move container to workspace $ws2

bindsym $mod+3          workspace number $ws3
bindsym $mod+Shift+3    move container to workspace $ws3; workspace $ws3
bindsym $mod+Ctrl+3     move container to workspace $ws3

bindsym $mod+4          workspace number $ws4
bindsym $mod+Shift+4    move container to workspace $ws4; workspace $ws4
bindsym $mod+Ctrl+4     move container to workspace $ws4

bindsym $mod+5          workspace number $ws5
bindsym $mod+Shift+5    move container to workspace $ws5; workspace $ws5
bindsym $mod+Ctrl+5     move container to workspace $ws5

bindsym $mod+6          workspace number $ws6
bindsym $mod+Shift+6    move container to workspace $ws6; workspace $ws6
bindsym $mod+Ctrl+6     move container to workspace $ws6

bindsym $mod+7          workspace number $ws7
bindsym $mod+Shift+7    move container to workspace $ws7; workspace $ws7
bindsym $mod+Ctrl+7     move container to workspace $ws7

bindsym $mod+8          workspace number $ws8
bindsym $mod+Shift+8    move container to workspace $ws8; workspace $ws8
bindsym $mod+Ctrl+8     move container to workspace $ws8

bindsym $mod+9          workspace number $ws9
bindsym $mod+Shift+9    move container to workspace $ws9; workspace $ws9
bindsym $mod+Ctrl+9     move container to workspace $ws9

bindsym $mod+0          mode "$mode_system"
# bindsym $mod+Shift+0
# bindsym $mod+Ctrl+0

# Open applications on specific workspaces
# assign [class="[...]"] $ws[...]
assign [class="Code"] $ws3
assign [class="Gimp"] $ws5
assign [class="Thunderbird"] $ws8
assign [class="Microsoft Teams - Preview"] $ws9

# Open specific applications in floating mode
# for_window [class="[...]"] floating enable
for_window [title="alsamixer"] floating enable
for_window [class="GParted"] floating enable
for_window [class="Lightdm-gtk-greeter-settings"] floating enable
for_window [class="Lxappearance"] floating enable
for_window [class="Nitrogen"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [class="Simple-scan"] floating enable
for_window [class="Xfburn"] floating enable

# #---Arrow key bindings---# #

bindsym $mod+Left           focus left
bindsym $mod+Shift+Left     move left 25
bindsym $mod+Ctrl+Left      workspace prev

bindsym $mod+Down           focus down
bindsym $mod+Shift+Down     move down 25
# bindsym $mod+Ctrl+Down

bindsym $mod+Up             focus up
bindsym $mod+Shift+Up       move up 25
# bindsym $mod+Ctrl+Up

bindsym $mod+Right          focus right
bindsym $mod+Shift+Right    move right 25
bindsym $mod+Ctrl+Right     workspace next

# #---XF86 key bindings---# #

# bindsym XF86AudioRaiseVolume
# bindsym XF86AudioLowerVolume
# bindsym XF86AudioMute
# bindsym XF86AudioMicMute
bindsym XF86MonBrightnessUp      exec --no-startup-id $HOME/.config/i3/scripts/brightness up
bindsym XF86MonBrightnessDown    exec --no-startup-id $HOME/.config/i3/scripts/brightness down
bindsym XF86Calculator           exec --no-startup-id mate-calculator

# #---Other key bindings---# #

bindsym Print                exec --no-startup-id flameshot gui
# bindsym $mod+Print
# bindsym $mod+Shift+Print

bindsym $mod+minus           scratchpad show
bindsym $mod+Shift+minus     move scratchpad

bindsym $mod+Tab             workspace next
bindsym $mod+Shift+Tab       workspace prev

bindsym $mod+Return          exec --no-startup-id $terminal
# bindsym $mod+Shift+Return

bindsym $mod+slash           exec --no-startup-id rofi -show drun
bindsym $mod+Shift+slash     exec --no-startup-id rofi -show window

bindsym $mod+space           focus mode_toggle
bindsym $mod+Shift+space     floating toggle

# #---Mode definitions---# #

# Set shut down, restart and locking features
set $mode_system [l]ock, [e]xit, switch_[u]ser, [s]uspend, [h]ibernate, [r]eboot, [S]hutdown

mode "$mode_system" {
    bindsym l exec --no-startup-id $HOME/.config/i3/scripts/i3exit lock, mode "default"
    bindsym s exec --no-startup-id $HOME/.config/i3/scripts/i3exit suspend, mode "default"
    bindsym u exec --no-startup-id $HOME/.config/i3/scripts/i3exit switch_user, mode "default"
    bindsym e exec --no-startup-id $HOME/.config/i3/scripts/i3exit logout, mode "default"
    bindsym h exec --no-startup-id $HOME/.config/i3/scripts/i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id $HOME/.config/i3/scripts/i3exit reboot, mode "default"
    bindsym Shift+s exec --no-startup-id $HOME/.config/i3/scripts/i3exit shutdown, mode "default"

    # Exit system mode: Enter or Escape or $mod+0
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+0 mode "default"
}

# Resize window (you can also use the mouse for that)
mode "resize" {
    # These bindings trigger as soon as you enter the resize mode

    # Pressing left will shrink the window’s width.
    # Pressing right will grow the window’s width.
    # Pressing up will shrink the window’s height.
    # Pressing down will grow the window’s height.
    bindsym h resize shrink width 10 px or 10 ppt
    bindsym j resize grow height 10 px or 10 ppt
    bindsym k resize shrink height 10 px or 10 ppt
    bindsym l resize grow width 10 px or 10 ppt

    # Same bindings, but for the arrow keys
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt

    # Back to normal: Enter or Escape or $mod+r
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

# Swap windows
set $mode_swap swap

mode "$mode_swap" {
    # Switch to workspace
    bindsym $mod+1 workspace $ws1
    bindsym $mod+2 workspace $ws2
    bindsym $mod+3 workspace $ws3
    bindsym $mod+4 workspace $ws4
    bindsym $mod+5 workspace $ws5
    bindsym $mod+6 workspace $ws6
    bindsym $mod+7 workspace $ws7
    bindsym $mod+8 workspace $ws8

    # Change focus
    bindsym $mod+h focus left
    bindsym $mod+j focus down
    bindsym $mod+k focus up
    bindsym $mod+l focus right
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right
    
    # Change focus (without mod)
    bindsym h focus left
    bindsym j focus down
    bindsym k focus up
    bindsym l focus right
    bindsym Left focus left
    bindsym Down focus down
    bindsym Up focus up
    bindsym Right focus right

    # Back to normal: Enter or Escape or $mod+Shift+s
    bindsym Return swap container with mark "swapee"; [con_mark="swapee"] focus; unmark "swapee"; mode "default";
    bindsym Escape unmark "swapee"; mode "default";
    bindsym $mod+Shift+s unmark "swapee"; mode "default";
}

# Press $mod+Shift+g to enter the gap mode. Choose o or i for modifying outer/inner gaps. Choose 0 to remove gaps or d to reset gaps for current workspace. Press one of + / - (in-/decrement for current workspace), 0 (remove gaps for current workspace), d (reset gaps for current workspace). If you also press Shift with these keys, the change will be global for all workspaces.
set $mode_gaps Gaps: [o]uter, [i]nner, [0] gaps, [d]efault
set $mode_gaps_outer Outer Gaps: [+], [-], [0], [d] (local); Shift + [+], [-], [0], [d] (global)
set $mode_gaps_inner Inner Gaps: [+], [-], [0], [d] (local); Shift + [+], [-], [0], [d] (global)

mode "$mode_gaps" {
    bindsym o      mode "$mode_gaps_outer"
    bindsym i      mode "$mode_gaps_inner"
    bindsym 0      mode "default", exec --no-startup-id i3-msg "gaps inner current set 0" && i3-msg "gaps outer current set 0"
    bindsym d      mode "default", exec --no-startup-id i3-msg "gaps inner current set $inner_gaps" && i3-msg "gaps outer current set $outer_gaps"

    # Back to normal: Enter or Escape or $mod+Shift+g
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+Shift+g mode "default"
}

mode "$mode_gaps_inner" {
    bindsym plus  gaps inner current plus 5
    bindsym minus gaps inner current minus 5
    bindsym 0     mode "default", gaps inner current set 0
    bindsym d     mode "default", gaps inner current set $inner_gaps

    bindsym Shift+plus  gaps inner all plus 5
    bindsym Shift+minus gaps inner all minus 5
    bindsym Shift+0     mode "default", gaps inner all set 0
    bindsym Shift+d     mode "default", gaps inner all set $inner_gaps

    # Back to normal: Enter or Escape or $mod+Shift+g
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+Shift+g mode "default"
}

mode "$mode_gaps_outer" {
    bindsym plus  gaps outer current plus 5
    bindsym minus gaps outer current minus 5
    bindsym 0     mode "default", gaps outer current set 0
    bindsym d     mode "default", gaps outer current set $outer_gaps

    bindsym Shift+plus  gaps outer all plus 5
    bindsym Shift+minus gaps outer all minus 5
    bindsym Shift+0     mode "default", gaps outer all set 0
    bindsym Shift+d     mode "default", gaps outer all set $outer_gaps

    # Back to normal: Enter or Escape or $mod+Shift+g
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+Shift+g mode "default"
}
